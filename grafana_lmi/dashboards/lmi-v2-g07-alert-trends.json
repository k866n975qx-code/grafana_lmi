{
  "__inputs": [],
  "__requires": [
    {
      "type": "grafana",
      "id": "grafana",
      "name": "Grafana",
      "version": "10.0.0"
    }
  ],
  "annotations": {
    "list": []
  },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "liveNow": false,
  "panels": [
    {
      "id": 1,
      "type": "timeseries",
      "title": "Alerts per Day by Category",
      "gridPos": {
        "x": 0,
        "y": 0,
        "w": 24,
        "h": 7
      },
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": "lmi_sqlite"
      },
      "targets": [
        {
          "refId": "A",
          "format": "table",
          "queryType": "table",
          "queryText": "SELECT (as_of_date_local || 'T00:00:00Z') AS time, SUM(CASE WHEN category='income' THEN 1 ELSE 0 END) AS income, SUM(CASE WHEN category='margin' THEN 1 ELSE 0 END) AS margin, SUM(CASE WHEN category='goal_pace' THEN 1 ELSE 0 END) AS goal_pace FROM alert_messages WHERE as_of_date_local >= date('now','-120 day') GROUP BY as_of_date_local ORDER BY as_of_date_local",
          "rawQueryText": "SELECT (as_of_date_local || 'T00:00:00Z') AS time, SUM(CASE WHEN category='income' THEN 1 ELSE 0 END) AS income, SUM(CASE WHEN category='margin' THEN 1 ELSE 0 END) AS margin, SUM(CASE WHEN category='goal_pace' THEN 1 ELSE 0 END) AS goal_pace FROM alert_messages WHERE as_of_date_local >= date('now','-120 day') GROUP BY as_of_date_local ORDER BY as_of_date_local"
        }
      ],
      "options": {
        "legend": {
          "displayMode": "list",
          "placement": "bottom",
          "showLegend": true
        },
        "tooltip": {
          "mode": "single",
          "sort": "none"
        }
      },
      "fieldConfig": {
        "defaults": {
          "unit": "none",
          "drawStyle": "line",
          "lineWidth": 2,
          "fillOpacity": 10,
          "showPoints": "never",
          "spanNulls": true,
          "decimals": 0
        },
        "overrides": []
      }
    },
    {
      "id": 2,
      "type": "timeseries",
      "title": "Notifications and Success %",
      "gridPos": {
        "x": 0,
        "y": 7,
        "w": 24,
        "h": 7
      },
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": "lmi_sqlite"
      },
      "targets": [
        {
          "refId": "A",
          "format": "table",
          "queryType": "table",
          "queryText": "WITH d AS (SELECT date(sent_at_utc) AS day, COUNT(*) AS sent_count, SUM(CASE WHEN success=1 THEN 1 ELSE 0 END) AS success_count FROM alert_notifications WHERE sent_at_utc >= datetime('now','-120 day') GROUP BY date(sent_at_utc)) SELECT (day || 'T00:00:00Z') AS time, sent_count AS \"Notifications\", CASE WHEN sent_count=0 THEN NULL ELSE (success_count*100.0/sent_count) END AS \"Success %\" FROM d ORDER BY day",
          "rawQueryText": "WITH d AS (SELECT date(sent_at_utc) AS day, COUNT(*) AS sent_count, SUM(CASE WHEN success=1 THEN 1 ELSE 0 END) AS success_count FROM alert_notifications WHERE sent_at_utc >= datetime('now','-120 day') GROUP BY date(sent_at_utc)) SELECT (day || 'T00:00:00Z') AS time, sent_count AS \"Notifications\", CASE WHEN sent_count=0 THEN NULL ELSE (success_count*100.0/sent_count) END AS \"Success %\" FROM d ORDER BY day"
        }
      ],
      "options": {
        "legend": {
          "displayMode": "list",
          "placement": "bottom",
          "showLegend": true
        },
        "tooltip": {
          "mode": "single",
          "sort": "none"
        }
      },
      "fieldConfig": {
        "defaults": {
          "unit": "none",
          "drawStyle": "line",
          "lineWidth": 2,
          "fillOpacity": 10,
          "showPoints": "never",
          "spanNulls": true,
          "decimals": 2
        },
        "overrides": []
      }
    }
  ],
  "refresh": "2m",
  "schemaVersion": 39,
  "style": "dark",
  "tags": [
    "lmi",
    "v2",
    "graph",
    "compact"
  ],
  "templating": {
    "list": []
  },
  "time": {
    "from": "now-120d",
    "to": "now"
  },
  "timepicker": {},
  "timezone": "browser",
  "title": "G07 LMI v2 - Alert Trends",
  "uid": "lmi-v2-g07-alert-trends",
  "version": 1,
  "weekStart": ""
}
