{
  "__inputs": [],
  "__requires": [
    {
      "type": "grafana",
      "id": "grafana",
      "name": "Grafana",
      "version": "10.0.0"
    }
  ],
  "annotations": {
    "list": []
  },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "liveNow": false,
  "panels": [
    {
      "id": 1,
      "type": "timeseries",
      "title": "Alerts per Day by Category",
      "gridPos": {
        "x": 0,
        "y": 0,
        "w": 12,
        "h": 8
      },
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": "lmi_sqlite"
      },
      "targets": [
        {
          "refId": "A",
          "format": "table",
          "queryType": "table",
          "queryText": "SELECT (as_of_date_local || 'T00:00:00Z') AS time, SUM(CASE WHEN category='income' THEN 1 ELSE 0 END) AS income, SUM(CASE WHEN category='margin' THEN 1 ELSE 0 END) AS margin, SUM(CASE WHEN category='goal_pace' THEN 1 ELSE 0 END) AS goal_pace, SUM(CASE WHEN category='ex_dividend' THEN 1 ELSE 0 END) AS ex_dividend FROM alert_messages WHERE as_of_date_local >= date('now','-120 day') GROUP BY as_of_date_local ORDER BY as_of_date_local",
          "rawQueryText": "SELECT (as_of_date_local || 'T00:00:00Z') AS time, SUM(CASE WHEN category='income' THEN 1 ELSE 0 END) AS income, SUM(CASE WHEN category='margin' THEN 1 ELSE 0 END) AS margin, SUM(CASE WHEN category='goal_pace' THEN 1 ELSE 0 END) AS goal_pace, SUM(CASE WHEN category='ex_dividend' THEN 1 ELSE 0 END) AS ex_dividend FROM alert_messages WHERE as_of_date_local >= date('now','-120 day') GROUP BY as_of_date_local ORDER BY as_of_date_local"
        }
      ],
      "options": {
        "legend": {
          "displayMode": "list",
          "placement": "bottom",
          "showLegend": true
        },
        "tooltip": {
          "mode": "single",
          "sort": "none"
        }
      },
      "fieldConfig": {
        "defaults": {
          "unit": "none",
          "drawStyle": "line",
          "lineWidth": 2,
          "fillOpacity": 10,
          "showPoints": "never",
          "spanNulls": true,
          "decimals": 0
        },
        "overrides": []
      }
    },
    {
      "id": 2,
      "type": "timeseries",
      "title": "Open Alert Count Trend",
      "gridPos": {
        "x": 12,
        "y": 0,
        "w": 12,
        "h": 8
      },
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": "lmi_sqlite"
      },
      "targets": [
        {
          "refId": "A",
          "format": "table",
          "queryType": "table",
          "queryText": "SELECT (as_of_date_local || 'T00:00:00Z') AS time, SUM(CASE WHEN status='open' THEN 1 ELSE 0 END) AS open_alerts FROM alert_messages WHERE as_of_date_local >= date('now','-120 day') GROUP BY as_of_date_local ORDER BY as_of_date_local",
          "rawQueryText": "SELECT (as_of_date_local || 'T00:00:00Z') AS time, SUM(CASE WHEN status='open' THEN 1 ELSE 0 END) AS open_alerts FROM alert_messages WHERE as_of_date_local >= date('now','-120 day') GROUP BY as_of_date_local ORDER BY as_of_date_local"
        }
      ],
      "options": {
        "legend": {
          "displayMode": "list",
          "placement": "bottom",
          "showLegend": true
        },
        "tooltip": {
          "mode": "single",
          "sort": "none"
        }
      },
      "fieldConfig": {
        "defaults": {
          "unit": "none",
          "drawStyle": "line",
          "lineWidth": 2,
          "fillOpacity": 10,
          "showPoints": "never",
          "spanNulls": true,
          "decimals": 0
        },
        "overrides": []
      }
    },
    {
      "id": 3,
      "type": "timeseries",
      "title": "Severity Trend",
      "gridPos": {
        "x": 0,
        "y": 8,
        "w": 12,
        "h": 8
      },
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": "lmi_sqlite"
      },
      "targets": [
        {
          "refId": "A",
          "format": "table",
          "queryType": "table",
          "queryText": "SELECT (as_of_date_local || 'T00:00:00Z') AS time, AVG(severity) AS avg_severity, MAX(severity) AS max_severity FROM alert_messages WHERE as_of_date_local >= date('now','-120 day') GROUP BY as_of_date_local ORDER BY as_of_date_local",
          "rawQueryText": "SELECT (as_of_date_local || 'T00:00:00Z') AS time, AVG(severity) AS avg_severity, MAX(severity) AS max_severity FROM alert_messages WHERE as_of_date_local >= date('now','-120 day') GROUP BY as_of_date_local ORDER BY as_of_date_local"
        }
      ],
      "options": {
        "legend": {
          "displayMode": "list",
          "placement": "bottom",
          "showLegend": true
        },
        "tooltip": {
          "mode": "single",
          "sort": "none"
        }
      },
      "fieldConfig": {
        "defaults": {
          "unit": "none",
          "drawStyle": "line",
          "lineWidth": 2,
          "fillOpacity": 10,
          "showPoints": "never",
          "spanNulls": true,
          "decimals": 2
        },
        "overrides": []
      }
    },
    {
      "id": 4,
      "type": "timeseries",
      "title": "Notifications and Success Ratio",
      "gridPos": {
        "x": 12,
        "y": 8,
        "w": 12,
        "h": 8
      },
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": "lmi_sqlite"
      },
      "targets": [
        {
          "refId": "A",
          "format": "table",
          "queryType": "table",
          "queryText": "WITH daily AS (SELECT date(sent_at_utc) AS day, COUNT(*) AS sent_count, SUM(CASE WHEN success=1 THEN 1 ELSE 0 END) AS success_count FROM alert_notifications WHERE sent_at_utc >= datetime('now','-120 day') GROUP BY date(sent_at_utc)) SELECT (day || 'T00:00:00Z') AS time, sent_count AS \"Notifications\", CASE WHEN sent_count=0 THEN NULL ELSE (success_count * 100.0 / sent_count) END AS \"Success %\" FROM daily ORDER BY day",
          "rawQueryText": "WITH daily AS (SELECT date(sent_at_utc) AS day, COUNT(*) AS sent_count, SUM(CASE WHEN success=1 THEN 1 ELSE 0 END) AS success_count FROM alert_notifications WHERE sent_at_utc >= datetime('now','-120 day') GROUP BY date(sent_at_utc)) SELECT (day || 'T00:00:00Z') AS time, sent_count AS \"Notifications\", CASE WHEN sent_count=0 THEN NULL ELSE (success_count * 100.0 / sent_count) END AS \"Success %\" FROM daily ORDER BY day"
        }
      ],
      "options": {
        "legend": {
          "displayMode": "list",
          "placement": "bottom",
          "showLegend": true
        },
        "tooltip": {
          "mode": "single",
          "sort": "none"
        }
      },
      "fieldConfig": {
        "defaults": {
          "unit": "none",
          "drawStyle": "line",
          "lineWidth": 2,
          "fillOpacity": 10,
          "showPoints": "never",
          "spanNulls": true,
          "decimals": 2
        },
        "overrides": []
      }
    },
    {
      "id": 5,
      "type": "timeseries",
      "title": "Reminder Frequency",
      "gridPos": {
        "x": 0,
        "y": 16,
        "w": 12,
        "h": 8
      },
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": "lmi_sqlite"
      },
      "targets": [
        {
          "refId": "A",
          "format": "table",
          "queryType": "table",
          "queryText": "SELECT (as_of_date_local || 'T00:00:00Z') AS time, SUM(reminder_count) AS reminders FROM alert_messages WHERE as_of_date_local >= date('now','-120 day') GROUP BY as_of_date_local ORDER BY as_of_date_local",
          "rawQueryText": "SELECT (as_of_date_local || 'T00:00:00Z') AS time, SUM(reminder_count) AS reminders FROM alert_messages WHERE as_of_date_local >= date('now','-120 day') GROUP BY as_of_date_local ORDER BY as_of_date_local"
        }
      ],
      "options": {
        "legend": {
          "displayMode": "list",
          "placement": "bottom",
          "showLegend": true
        },
        "tooltip": {
          "mode": "single",
          "sort": "none"
        }
      },
      "fieldConfig": {
        "defaults": {
          "unit": "none",
          "drawStyle": "line",
          "lineWidth": 2,
          "fillOpacity": 10,
          "showPoints": "never",
          "spanNulls": true,
          "decimals": 0
        },
        "overrides": []
      }
    }
  ],
  "refresh": "2m",
  "schemaVersion": 39,
  "style": "dark",
  "tags": [
    "lmi",
    "v2",
    "graph",
    "alerts"
  ],
  "templating": {
    "list": []
  },
  "time": {
    "from": "now-120d",
    "to": "now"
  },
  "timepicker": {},
  "timezone": "browser",
  "title": "G07 LMI v2 - Alert Trends",
  "uid": "lmi-v2-g07-alert-trends",
  "version": 1,
  "weekStart": ""
}
